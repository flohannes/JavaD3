const path = require("path")

const EJS_DIR = path.join(__dirname, "ejs")
const LIBS_DIR = path.join(__dirname, "libs")
const CSS_DIR = path.join(__dirname, "css")

module.exports = {EJS_DIR : EJS_DIR,
                 LIBS_DIR : LIBS_DIR,
                 CSS_DIR : CSS_DIR}